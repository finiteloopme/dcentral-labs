# genai-chain-dev Configuration
# Copy to .env and customize for your environment
#
# Usage:
#   make check-env CHAIN=somnia    # Validate configuration
#   make cloud-deploy CHAIN=somnia # Deploy to GCP

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Google Cloud Project ID
PROJECT_ID=your-gcp-project-id

# GCS bucket for Terraform state storage
# Will be created by 'make check-env' if it doesn't exist
STATE_BUCKET=your-project-terraform-state

# Cloud Build service account email (user-managed, not default)
# See README.md for bootstrap instructions
CLOUDBUILD_SA_EMAIL=chain-dev-cloudbuild-sa@your-project.iam.gserviceaccount.com

# =============================================================================
# OPTIONAL CONFIGURATION (sane defaults)
# =============================================================================

# GCP region for all resources
REGION=us-central1

# Terraform state prefix in bucket
STATE_PREFIX=terraform/state

# Container image tag
TAG=latest

# =============================================================================
# VERTEX AI (for OpenCode)
# =============================================================================

# Vertex AI project (if different from PROJECT_ID)
# VERTEX_AI_PROJECT=

# Vertex AI region (default: global)
# VERTEX_LOCATION=global

# =============================================================================
# LOCAL DEVELOPMENT OVERRIDES
# =============================================================================
# These override the baked-in chain configuration when running locally.
# Useful for connecting to local Anvil forks or different testnet endpoints.

# Override RPC URL (e.g., for local Anvil fork)
# RPC_URL=http://localhost:8545

# Override Chain ID (e.g., for local Anvil)
# CHAIN_ID=31337

# Override Explorer URL
# EXPLORER_URL=

# Override Faucet URL
# FAUCET_URL=
