# Development scratchpad

```bash
pnpm init
pnpm install express ethers@^6 dotenv cors @google/generative-ai web3-utils # Using web3-utils for solidityKeccak256, ethers has similar
pnpm install -D typescript @types/express @types/node @types/cors ts-node nodemon
```