# Coding Labs - Session Context

> This file maintains context between coding sessions. Read this at the start of each session.

## Project Overview

- **Purpose:** Multi-agent platform for on-chain development using A2A (Agent-to-Agent) protocol
- **Tech Stack:** TypeScript, pnpm workspaces, A2A SDK (@a2a-js/sdk), AI SDK (ai@^5.0 + @ai-sdk/google-vertex@^3.0), MCP, viem
- **Testing:** Vitest
- **Linting:** ESLint + Prettier
- **Containers:** Podman
- **Status:** Phase 2 - Core Skills Implemented (deploy, tx-status, query-state, Midnight deploy/call with full ZK circuit execution)

## Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│           Container: opencode-web (port 3000)                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    "opencode web"                             │  │
│  │              (Primary User Interface)                         │  │
│  │                                                               │  │
│  │  - Browser-based coding session                               │  │
│  │  - Wallet connection (injected provider / MetaMask)           │  │
│  │  - A2A Client integrated                                      │  │
│  │  - Routes to chain agents based on user intent                │  │
│  └───────────────────────────────┬───────────────────────────────┘  │
└──────────────────────────────────┼──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│           Container: agent-registry (port 4000)                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Agent Registry - Central Directory                           │  │
│  │  - Reads from config.toml                                     │  │
│  │  - GET /agents → List enabled agents with URLs                │  │
│  │  - GET /health → Health check                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │ Returns agent URLs
           ┌───────────────────────┼───────────────────────────────┐
           ▼                       ▼                               ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│ Container:        │   │ Container:        │   │ Container:        │
│ somnia-agent      │   │ sonic-agent       │   │ midnight-agent    │
│ (port 4001)       │   │ (port 4002)       │   │ (port 4003)       │
├───────────────────┤   ├───────────────────┤   ├───────────────────┤
│ TypeScript A2A    │   │ TypeScript A2A    │   │ TypeScript A2A    │
│ Server            │   │ Server            │   │ Server            │
│                   │   │                   │   │                   │
│ LLM: Claude Opus  │   │ LLM: Claude Opus  │   │ LLM: Claude Opus  │
│ 4.5 (Vertex AI)   │   │ 4.5 (Vertex AI)   │   │ 4.5 (Vertex AI)   │
│ via AI SDK        │   │ via AI SDK        │   │ via AI SDK        │
│                   │   │                   │   │                   │
│ Skills:           │   │ Skills:           │   │ Skills:           │
│ - solidity-gen    │   │ - solidity-gen    │   │ - compact-gen     │
│ - deploy          │   │ - compile ←───────┼──┐│ - compile         │
│ - tx-status       │   │ - deploy ←────────┼──┤│ - deploy ←────────┼──┐
│ - query-state     │   │ - call ←──────────┼──┤│ - call ←──────────┼──┤
│ - reactivity      │   │ - tx-status ←─────┼──┤│ - query-state     │  │ uses
│ - data-streams    │   │ - feem-info       │  ││ - private-state   │  │ MCP
└───────────────────┘   └─────────┬─────────┘  │└───────────────────┘  │ tools
                                  │ uses MCP   │        │              │
                                  ▼            │        ▼              │
                        ┌───────────────────┐  │┌───────────────────┐  │
                        │ Container:        │  ││ Container:        │  │
                        │ evm-mcp           │←─┘│ midnight-mcp      │←─┘
                        │ (port 4011)       │   │ (port 4010)       │
                        ├───────────────────┤   ├───────────────────┤
                        │ MCP Server (SSE)  │   │ MCP Server (SSE)  │
                        │                   │   │                   │
                        │ Tools:            │   │ Tools:            │
                        │ - forge_compile   │   │ - compact_compile │
                        │ - forge_deploy    │   │ - compact_validate│
                        │ - cast_call       │   │ - contract_deploy │
                        │ - cast_send       │   │ - contract_call   │
                        │ - cast_receipt    │   │ - wallet_create   │
                        │ - chain_start     │   │ - query_contract  │
                        │ - chain_stop      │   │ - query_blocks    │
                        │                   │   │ - zk_prove        │
                        │ Resources:        │   │ - request_tokens  │
                        │ - chains          │   │                   │
                        │ - foundry-version │   │ Resources:        │
                        └───────────────────┘   │ - networks        │
                                                │ - compatibility   │
                                                │ - docker-images   │
                                                └───────────────────┘
```

## Centralized Configuration

All service configuration is managed via `config.toml` at the project root.

### Configuration Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                        config.toml                                   │
│                    (Source of Truth)                                 │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────────┐
│ Local Dev     │   │ Containers    │   │ Cloud Run         │
│               │   │               │   │                   │
│ Config loader │   │ .env.generated│   │ Env vars set via  │
│ reads TOML    │   │ + compose.yaml│   │ gcloud/terraform  │
│ directly      │   │               │   │                   │
└───────────────┘   └───────────────┘   └───────────────────┘
```

### Config Resolution Order (later wins)

1. `[default.*]` sections in config.toml
2. `[production.*]` sections (environment-specific)
3. Environment variables (`SOMNIA_AGENT_HOST`, `SOMNIA_AGENT_PORT`, `SOMNIA_AGENT_URL`, etc.)

### Key Files

| File | Purpose |
|------|---------|
| `config.toml` | Master configuration - services, agents, networks, LLM |
| `scripts/generate-env.ts` | Generates `.env.generated` from config.toml |
| `.env.generated` | Auto-generated env file for compose.yaml |
| `packages/shared/src/config/` | TypeScript config loader module |
| `packages/agent-registry/` | Service that reads config and serves agent list |

## Key Decisions

| Decision | Choice | Rationale | Date |
|----------|--------|-----------|------|
| Language | TypeScript | Mature A2A SDK, good LLM/AI integrations | 2026-02-11 |
| A2A SDK | @a2a-js/sdk v0.3.10 | Official SDK, full protocol support | 2026-02-11 |
| Monorepo | pnpm workspaces | Fast, disk-efficient, strict deps | 2026-02-11 |
| LLM Provider | Vertex AI (Claude Opus 4.5) | AI SDK + @ai-sdk/google-vertex, existing GCP setup | 2026-02-11 |
| First Agent | Somnia | High-perf EVM L1, unique features | 2026-02-11 |
| Transport | HTTP+JSON (REST) | Easy debugging, curl-friendly | 2026-02-11 |
| Dev Containers | Podman + Compose | Local development | 2026-02-11 |
| Prod Deploy | Cloud Run | Scalable, serverless | 2026-02-11 |
| Wallet Connect | Injected provider (MVP) | Simple, no external deps | 2026-02-11 |
| Signing Flow | Unsigned tx → User signs | Security: private key stays in wallet | 2026-02-11 |
| Testing | Vitest | Fast, native TS support | 2026-02-11 |
| Linting | ESLint + Prettier | Industry standard | 2026-02-11 |
| Operations | Makefile + shell scripts | Simple, modular | 2026-02-11 |
| OpenCode | Forked v1.1.56 | Wallet UI + A2A plugin requires source mods | 2026-02-11 |
| OpenCode Integration | Baked-in plugin | Start simple, dynamic loading as future TODO | 2026-02-11 |
| Agent Discovery | Static registry + service | Admin-managed config.toml, registry service | 2026-02-12 |
| Configuration | Centralized config.toml | Single source of truth for all services | 2026-02-12 |
| LLM Framework | AI SDK (ai@^5.0) | Replaced Genkit; cleaner API, better model support | 2026-02-14 |
| Agent LLM | Claude Opus 4.5 via Vertex AI | claude-opus-4-5 model ID, both agents | 2026-02-14 |
| Midnight Tooling | MCP Server (midnight-mcp) | Combined MCP server for Midnight infra, SSE transport | 2026-02-14 |
| EVM Tooling | MCP Server (evm-mcp) | Shared Foundry toolchain for EVM agents, SSE transport | 2026-02-18 |

## Somnia Chain Info

Somnia is a high-performance EVM-compatible L1:
- **Performance:** 1,000,000+ TPS, sub-second finality
- **Consensus:** MultiStream Consensus
- **Mainnet:** Chain ID 5031, RPC: https://api.infra.mainnet.somnia.network/
- **Testnet (Shannon):** Chain ID 50312, RPC: https://dream-rpc.somnia.network/
- **Native Token:** SOMI (mainnet), STT (testnet)
- **Unique Features:** Reactivity (on-chain pub/sub), Data Streams

### Gas Considerations
- LOG operations: Higher cost (8,320+ gas vs Ethereum's 631)
- Contract deploy: 3,125 gas/byte (vs 200)
- ecRecover: 150,000 gas (vs 3,000)
- Cold storage: +1M gas penalty outside 128M key cache

## Sonic Chain Info

Sonic is a high-performance EVM-compatible L1 (rebranded from Fantom):
- **Performance:** 400,000+ TPS, sub-second finality
- **Chain ID:** 146
- **RPC:** https://rpc.soniclabs.com
- **Explorer:** https://sonicscan.org
- **Native Token:** S (Sonic)
- **Unique Features:** Fee Monetization (FeeM) - dApps earn 90% of gas fees

### FeeM (Fee Monetization)
- Register contracts with FeeM program
- Earn 90% of transaction fees from registered contracts
- Requires: Implementation address, app name, metadata URI

## Implementation Phases

### Phase 1: Foundation (MVP) - COMPLETE
- [x] Create .context.md
- [x] Initialize pnpm monorepo
- [x] Create shared package (types, base classes)
- [x] Create somnia-agent with SKILLS.md
- [x] Implement AgentCard
- [x] Implement solidity-gen skill
- [x] Containerfile and compose.yaml
- [x] Test with curl / A2A CLI
- [x] Enable OpenCode web with wallet connection (injected provider)

### Phase 1.5: OpenCode Integration - COMPLETE
- [x] Fork OpenCode v1.1.56 (branch: feature/a2a-wallet-integration)
- [x] Create wallet context (SolidJS) with viem
- [x] Create wallet button component for header
- [x] Create A2A plugin with:
  - [x] Agent registry (Somnia, Sonic, Midnight)
  - [x] Intent router (keyword/chain/default routing)
  - [x] A2A client wrapper
  - [x] Custom tools: a2a-send, a2a-agents, a2a-capabilities
- [x] Register A2A plugin in OpenCode INTERNAL_PLUGINS
- [x] Create Containerfile.web for OpenCode console
- [x] Update compose.yaml with opencode-web service
- [x] Update Makefile & scripts
- [ ] Create fork on GitHub (dcentral-labs/opencode)
- [ ] Push feature branch to fork

### Phase 2: Full Skill Implementation - IN PROGRESS
- [x] Implement deploy skill (viem + wallet)
- [x] Implement tx-status skill
- [x] Implement query-state skill
- [x] Fix GCloud credentials in containers (ADC mount)
- [ ] Add streaming for code generation
- [ ] Test full flow: generate → deploy → check status

### Phase 3: Somnia-Specific & Multi-Agent
- [ ] Implement reactivity-setup skill
- [ ] Implement data-streams skill
- [ ] Add RAG with Somnia docs
- [ ] Build a2a-client package
- [x] **Add Sonic agent** (high-perf EVM L1 with FeeM)
  - [x] Create evm-mcp server (Foundry toolchain MCP, port 4011)
  - [x] Create sonic-agent with 6 skills (solidity-gen, compile, deploy, call, tx-status, feem-info)
  - [x] Integrate via MCP client (@modelcontextprotocol/sdk)
  - [ ] Implement FeeM registration (TODO)
- [x] **Add Midnight agent** (privacy-focused chain with Compact language)
  - [x] Set up Midnight.js SDK integration (via midnight-mcp)
  - [x] Install Compact compiler in container
  - [x] Add Proof Server (Docker-in-Docker or sidecar)
  - [x] Implement compact-gen skill (LLM generates Compact code)
  - [x] Implement compile skill (shell to `compact compile`)
  - [x] Implement deploy skill (Midnight.js + headless wallet + auto-funding)
  - [x] Implement call skill (execute circuit with ZK proof)
  - [ ] Implement query-state skill (Indexer GraphQL) - stub exists
  - [ ] Add RAG with Midnight docs

### Phase 4: Production - IN PROGRESS
- [x] Cloud Run deployment (Cloud Build + Cloud Run)
- [x] Google authentication via IAP (Identity-Aware Proxy)
- [ ] Authentication between agents (TODO: lock down internal services)
- [ ] Monitoring & logging
- [ ] Documentation

## Session Log

### Next Session
- **Test sonic-agent compile/deploy/call via evm-mcp** - Full flow with Anvil
- **Implement github-agent** - Git integration via Developer Connect
- **Full end-to-end test via OpenCode web UI** - Test generate → compile → deploy → call → query-state with session persistence
- **Add session persistence** - Current in-memory session state is lost on container restart

### 2026-02-19 (session 24)
- **Fixed Binary Artifact Encoding Bug in Midnight Deploy Pipeline:**
  - **Root Cause:** Binary ZK key files (`.prover`, `.verifier`, `.bzkir`) were being written as base64 TEXT instead of being decoded to binary
  - This caused `generate-intent deploy` to fail silently when parsing malformed key files
  - **Issue Chain:**
    1. MCP compile tool correctly reads binary files and base64-encodes them with `mimeType: 'application/octet-stream;base64'`
    2. Agent's `mcp-client.ts` parsed compile response but **discarded mimeType** (Artifact interface only had `filename` and `content`)
    3. MCP deploy tool received artifacts **without mimeType**
    4. `writeArtifacts()` checked `mimeType?.includes('base64')` which was `false`
    5. Binary files written as base64 text → toolkit failed to parse them
- **Fix 1: `packages/midnight-mcp/src/tools/contract.ts`** (already in source, needed container rebuild):
  - Added binary file detection by extension as fallback:
    ```typescript
    const isBinaryExtension =
      filename.endsWith('.prover') ||
      filename.endsWith('.verifier') ||
      filename.endsWith('.bzkir');
    const isBase64Binary = mimeType?.includes('base64') || isBinaryExtension;
    ```
- **Fix 2: `packages/midnight-agent/src/mcp-client.ts`:**
  - Added `mimeType?: string` to `Artifact` interface
  - Updated regex parsing to capture mimeType (was `match[2]` but never stored)
- **Fix 3: `packages/midnight-agent/src/skills/index.ts`:**
  - Added `mimeType?: string` to `Artifact` interface for consistency
- **E2E Test Results (All Passed):**
  | Step | Status | Details |
  |------|--------|---------|
  | Compile | ✅ | 12 artifacts generated |
  | Deploy | ✅ | Contract: `4e7ccd934f5c832e23b07a2751c60700df8526be6be831376ac92a57e6f4d259` |
  | Call increment | ✅ | Circuit executed successfully |
  | Query state | ✅ | Ledger state retrieved |
- **Binary Files Now Correctly Decoded:**
  - Logs show `Wrote binary artifact:` instead of `Wrote artifact:` for key files
  - Example: `increment.prover (14071 bytes)`, `increment.verifier (1351 bytes)`
- **Files Modified:**
  - `packages/midnight-mcp/src/tools/contract.ts` - Binary extension detection (was in source, needed rebuild)
  - `packages/midnight-agent/src/mcp-client.ts` - Added mimeType to Artifact interface and capture
  - `packages/midnight-agent/src/skills/index.ts` - Added mimeType to Artifact interface

### 2026-02-19 (session 23)
- **Investigated IAP External User Access (Argolis Limitation):**
  - Root cause: `kunall.altostrat.com` is an Argolis (Google internal demo) org
  - Argolis orgs have built-in restrictions preventing external users from accessing IAP-protected resources
  - This is enforced at Google's infrastructure level, not configurable via IAM/org policies
  - Evidence: OAuth identifies user (shows email on error page) but Cloud Audit Logs show empty `authenticationInfo`
  - Only `@kunall.altostrat.com` users can access IAP-protected Cloud Run services
- **Cleaned Up Failed Access Level Attempt:**
  - Deleted `dcoder_allowed_users` access level from Access Context Manager
  - Removed `sync-access-level` command from `scripts/cloud.sh`
  - Removed `cloud-sync-access-level` target from `Makefile`
  - Reverted `config.toml` to single admin user with Argolis limitation note
- **Fixed Cloud Build Failures:**
  - `deploy-midnight-proof-server`: Removed reserved `PORT=6300` env var (Cloud Run sets PORT automatically)
  - `build-midnight-mcp`: Added `toolkit` stage alias to fix ARG substitution in `COPY --from=`
  - `deploy-evm-mcp`: Changed to `npm install --production` to properly install dependencies (pnpm symlinks don't work in containers)
- **Fixed Agents Dropdown Empty in Cloud Run:**
  - Root cause: Frontend called `VITE_AGENT_REGISTRY_URL` which defaulted to `localhost:4000`
  - Solution: Added backend proxy endpoints `/global/registry/agents` and `/global/registry/agents/health`
  - Updated frontend `agents.ts` to accept `serverUrl` parameter
  - Updated `agents.tsx` context to use `useServer()` for server URL
  - Frontend now proxies through OpenCode backend → agent-registry service
- **Fixed Agent Picker Toggle Not Working:**
  - Root cause: Double toggle - button `onClick` AND Checkbox `onChange` both called `toggleAgent()`
  - Solution: Removed `onChange` handler from Checkbox in `agent-picker.tsx`
- **Fixed Midnight Agent Defaulting to Preview Instead of Local:**
  - Root cause: `detectNetwork()` in agent skills defaulted to `'preview'` instead of `'local'`
  - Updated 3 files in `packages/midnight-agent/src/skills/`:
    - `deploy.ts`: Changed default from `'preview'` to `'local'`
    - `call.ts`: Changed default from `'preview'` to `'local'`
    - `query-state.ts`: Changed default from `'preview'` to `'local'`
  - Updated `agent-card.ts`: Deploy skill description now mentions "local dev chain" as default
  - Updated `packages/midnight-mcp/src/tools/index.ts`: `networkEnum` description says "Defaults to local dev chain"
- **Files Modified:**
  - `cloudbuild.yaml` - Removed reserved PORT env var from proof-server deploy
  - `packages/midnight-mcp/Containerfile` - Added toolkit stage alias for ARG substitution
  - `packages/evm-mcp/Containerfile` - Use npm install instead of copying pnpm node_modules
  - `opencode/packages/opencode/src/server/routes/global.ts` - Added registry proxy endpoints
  - `opencode/packages/app/src/lib/agents.ts` - Updated to use server URL parameter
  - `opencode/packages/app/src/context/agents.tsx` - Use server context for API calls
  - `opencode/packages/app/src/components/agent-picker.tsx` - Removed duplicate onChange handler
  - `packages/midnight-agent/src/skills/deploy.ts` - Default to local network
  - `packages/midnight-agent/src/skills/call.ts` - Default to local network
  - `packages/midnight-agent/src/skills/query-state.ts` - Default to local network
  - `packages/midnight-agent/src/agent-card.ts` - Updated deploy skill description
  - `packages/midnight-mcp/src/tools/index.ts` - Updated networkEnum description
  - `scripts/cloud.sh` - Removed ACM sync command
  - `Makefile` - Removed cloud-sync-access-level target
  - `config.toml` - Reverted to single user, added Argolis note
- **Cloud Run Infrastructure:**
  - `midnight-node` already runs with `CFG_PRESET=dev` (ephemeral dev chain from genesis)
  - `midnight-mcp` has `MIDNIGHT_DEFAULT_NETWORK=local` pointing to Cloud Run infrastructure
  - Local chain is pre-funded with genesis wallet (no faucet needed)
- **Build Submitted:** `ed1c37c4-4b7d-44ae-abab-d0c38539ca11`

### 2026-02-19 (session 22)
- **Fixed Midnight MCP Circuit Calls:**
  - **Root Cause:** `generateCircuitIntent` was NOT running from `/toolkit-js` directory
  - The toolkit's TypeScript runtime requires configs to be in `/toolkit-js` to resolve `@midnight-ntwrk/*` modules
  - `generateDeployIntent` had this fix, but `generateCircuitIntent` was missed
- **Updated `toolkit-client.ts`:**
  - Rewrote `generateCircuitIntent()` to match `generateDeployIntent()` pattern:
    - Create temp dir in `/toolkit-js` (`circuit-{uuid}`)
    - Copy outputDir, configPath, onchainStateFile, privateStateFile to toolkit work dir
    - Run `generate-intent circuit` with `{ cwd: TOOLKIT_JS_PATH }`
    - Copy output files back to original outputDir
    - Clean up temp directory on success
  - Added `from circuit-{tempName}` to log message for debugging
- **Updated `contract.ts` (`callContractCircuit`):**
  - Added Step 0: Copy `managed/` folder from cached deployment to call work directory
  - Copy `contract.config.ts` to work directory
  - Use copied compiledDir for `send-intent` (not cached path)
  - This ensures all files are colocated for toolkit-js resolution
- **Updated `compose.yaml`:**
  - Added `COMPACTC_SKIP_ZK=false` to midnight-mcp environment
  - Enables full ZK key generation during compilation (required for deployment)
- **Fixed Container Image Mismatch:**
  - podman-compose was building from cached layers
  - Running container had different image ID than `midnight-mcp:latest`
  - Solution: Force `podman rmi` + `podman build --no-cache` + manual `podman run`
- **Verified End-to-End Flow:**
  - Deploy: Contract deployed successfully with 16 artifacts (including ZK keys)
  - Call read_count: ✅ Circuit called successfully
  - Call increment: ✅ Circuit called successfully  
  - Call read_count (after): ✅ Circuit called successfully
  - State verified on chain via `contract-state` command
- **Files Modified:**
  - `packages/midnight-mcp/src/toolkit-client.ts` - Fixed `generateCircuitIntent`
  - `packages/midnight-mcp/src/tools/contract.ts` - Fixed `callContractCircuit`
  - `compose.yaml` - Added `COMPACTC_SKIP_ZK=false`
  - `packages/midnight-mcp/test-deploy.ts` - Updated port to 8080
  - `packages/midnight-mcp/test-call.ts` - Updated port to 8080
- **Architecture (Circuit Call Flow):**
  ```
  1. callContractCircuit receives contract address + circuit name
  2. Step 0: Copy managed/ folder to call work directory
  3. Step 1: Fetch on-chain state via toolkit contract-state
  4. Step 2: Load private state from /data/state/{address}.json
  5. Step 3: Get coin public key via show-address
  6. Step 4: Generate circuit intent (runs from /toolkit-js)
     - Copies files to /toolkit-js/circuit-{uuid}/
     - Runs generate-intent circuit with cwd=/toolkit-js
     - Copies output back to work directory
  7. Step 5: Send intent via send-intent (creates ZK proof)
  8. Step 6: Update private state
  ```

### 2026-02-18 (session 21)
- **Implemented evm-mcp MCP Server (`packages/evm-mcp/`):**
  - Foundry toolchain server for EVM-compatible chains
  - Port 4011, SSE transport over HTTP
  - 7 MCP tools: `forge_compile`, `forge_deploy`, `cast_call`, `cast_send`, `cast_receipt`, `chain_start`, `chain_stop`
  - 2 resources: `evm://chains`, `evm://foundry-version`
  - Chain lifecycle: explicit start/stop for Anvil instances
  - Files: package.json, tsconfig.json, Containerfile, src/index.ts, src/config.ts, src/tools/*.ts
- **Implemented sonic-agent A2A Server (`packages/sonic-agent/`):**
  - Full A2A server for Sonic blockchain (high-perf EVM L1)
  - Port 4002, LLM: Claude Opus 4.5 via Vertex AI
  - 6 skills: solidity-gen (LLM), compile, deploy, call, tx-status (→ evm-mcp), feem-info (stub)
  - Uses MCP client (`@modelcontextprotocol/sdk`) to connect to evm-mcp
  - Files: package.json, tsconfig.json, Containerfile, SKILLS.md, src/*.ts, src/skills/*.ts
- **Updated Infrastructure:**
  - `compose.yaml` - Added evm-mcp and sonic-agent services with dependencies
  - `cloudbuild.yaml` - Added build/push/deploy steps for both services
  - `scripts/cloud.sh` - Added evm-mcp and sonic-agent to urls/delete commands
  - `config.toml` - Enabled sonic agent
- **Model Fix:**
  - `claude-3-5-sonnet-v2@20241022` not available on Vertex AI
  - Changed to `claude-opus-4-5` (same as midnight-agent)
- **Local Testing:**
  - evm-mcp: http://localhost:4011 - health endpoint working
  - sonic-agent: http://localhost:4002 - health and agent card working
  - A2A code generation tested successfully (Counter contract generated with OpenZeppelin imports)
- **Git:**
  - Branch: `evm-mcp`
  - 3 commits pushed:
    1. `feat(evm-mcp): implement MCP server for Foundry toolchain`
    2. `feat(sonic-agent): implement Sonic A2A agent with evm-mcp integration`
    3. `fix(sonic-agent): use claude-opus-4-5 model (same as midnight-agent)`

### 2026-02-18 (session 20)
- **Fixed Midnight MCP Multi-Connection Crash:**
  - **Root cause:** Single `McpServer` instance was shared across all SSE connections
  - MCP SDK limitation: `McpServer` can only connect to ONE transport at a time
  - When midnight-agent reconnected, server crashed with "Already connected to a transport"
  - **Fix:** Refactored `packages/midnight-mcp/src/index.ts` to create new `McpServer` per SSE connection
  - Added `createMcpServer()` factory function that registers tools/resources on fresh instance
  - Changed `transports` map to `sessions` map tracking both transport and server per connection
  - Added session logging and `activeSessions` count to health endpoint
- **Fixed Compact Language Version:**
  - Updated `packages/midnight-agent/SKILLS.md`: `pragma language_version 0.21` → `0.20` (4 occurrences)
  - Updated `packages/midnight-agent/src/skills/compact-gen.ts`: system prompt and fallback context → `0.20`
  - compactc 0.28.0 requires language version 0.20, not 0.21
- **Verified Fix:**
  - Rebuilt midnight-mcp and midnight-agent containers
  - Tested 4 sequential MCP connections - all compiled successfully
  - No more "exit code 255" errors
  - Logs show proper connection lifecycle: "New SSE connection" → compile → "SSE connection closed"

### 2026-02-18 (session 19)
- **Installed Compact Compiler (compactc 0.28.0) in midnight-mcp Container:**
  - **Problem:** The `midnight-node-toolkit` does NOT include the `compactc` compiler (contrary to earlier assumption)
  - **Solution:** Download compactc directly from GitHub releases to `/usr/local/bin/`
  - Files in compactc zip: `compactc` (wrapper), `compactc.bin` (binary), `fixup-compact`, `format-compact`, `zkir`
  - Added `COMPACTC_VERSION=0.28.0` build arg to Containerfile
  - Verified: `compactc --version` outputs `0.28.0` in container
- **Updated compile.ts to Use compactc Directly:**
  - Removed `runToolkit(['compactc', ...])` approach (toolkit doesn't have compactc subcommand)
  - Created `runCompactc()` function that spawns compactc binary directly
  - Added `COMPACTC_PATH` env var support (defaults to `compactc` in PATH)
  - Added `COMPACTC_SKIP_ZK` env var support (defaults to true for faster compilation)
  - Updated artifact discovery to match compactc output structure:
    - `contract/index.js`, `contract/index.d.ts`, `contract/index.js.map`
    - `zkir/*.zkir`
    - `compiler/contract-info.json`
- **Fixed TypeScript Build Scripts (pnpm exec tsc):**
  - **Root Cause:** Build scripts used `node ./node_modules/typescript/lib/tsc.js` which works locally but fails in containers
  - When pnpm hoists dependencies, TypeScript is at workspace root, not in each package's `node_modules/`
  - Additionally, `COPY packages/<pkg>/` was copying local `node_modules/` with broken symlinks into container
  - **Solution 1:** Changed all `package.json` build scripts to use `pnpm exec tsc` (works with pnpm workspace hoisting)
  - **Solution 2:** Created `.dockerignore` to exclude `**/node_modules` and `**/dist` from COPY
- **Files Modified:**
  - `packages/midnight-mcp/Containerfile` - Added compactc 0.28.0 installation, uses `npx tsc` for build
  - `packages/midnight-mcp/src/tools/compile.ts` - Rewrote to spawn compactc directly
  - `packages/midnight-mcp/src/config.ts` - Added `COMPACTC_VERSION` constant, fixed comments
  - `packages/shared/package.json` - Build script → `pnpm exec tsc`
  - `packages/agent-registry/package.json` - Build script → `pnpm exec tsc`
  - `packages/somnia-agent/package.json` - Build script → `pnpm exec tsc`
  - `packages/midnight-agent/package.json` - Build script → `pnpm exec tsc`
  - `packages/midnight-mcp/package.json` - Build script → `pnpm exec tsc`
  - `.dockerignore` - **NEW** - Excludes node_modules, dist, .git from container builds
- **Container Builds Verified:**
  - `midnight-mcp:test` - ✅ Builds with compactc 0.28.0, compilation tested successfully
  - `agent-registry:test` - ✅ Builds with pnpm exec tsc
  - `somnia-agent:test` - ✅ Builds with pnpm exec tsc
  - `midnight-agent:test` - ✅ Builds with pnpm exec tsc
- **Compact Compilation Tested:**
  - Sample contract compiled successfully in container
  - Language version: `pragma language_version 0.20;` (compactc 0.28.0 requires 0.20, not 0.21)
  - Output: `contract/index.js` (13KB), `contract/index.d.ts`, `zkir/*.zkir`, `compiler/contract-info.json`
- **Compatibility Matrix Verified:**
  | Component | Version |
  |-----------|---------|
  | Node + Toolkit | 0.20.1 |
  | Compact Compiler | 0.28.0 |
  | Compact Language | 0.20 |
  | Proof Server | 7.0.0 |
  | Indexer | 3.0.0 |

### 2026-02-18 (session 18)
- **Deployed all infrastructure to Cloud Run:**
  - All 6 services deployed and healthy
  - Verified MCP integration: midnight-agent → midnight-mcp working in production
  - Compact compilation successful with `pragma language_version 0.21;`
- **Planned GitHub Integration:**
  - Documented full plan in `GITHUB-INTEGRATION.md`
  - Architecture: New `github-agent` (port 4004) for git-save, git-list, git-load
  - Uses GCP Developer Connect for GitHub auth (no PAT management)
  - Monorepo structure: `finiteloopme/dcoder/{username}-{project}/`
  - A2A delegation: Chain agents pass artifacts to github-agent
- **Planned EVM MCP Server:**
  - Documented full plan in `packages/evm-mcp/README.md`
  - Architecture: Shared Foundry toolchain for all EVM agents
  - Tools: forge_compile, forge_deploy, cast_call, cast_send, cast_receipt
  - Session locking: One EVM agent per session
  - Default: Local Anvil fork (forks from real chain RPC)
  - Explicit opt-in for mainnet deployments (returns unsigned tx)
- **Updated Sonic Agent Plan:**
  - Refactored `SONIC-AGENT.md` to use evm-mcp
  - Thin wrapper: LLM for code gen + MCP for Foundry operations
  - Skills: solidity-gen (LLM), compile/deploy/call (→ evm-mcp), feem-info
- **Key Architectural Decisions:**
  - Single Anvil per evm-mcp instance (explicit error if chain switch attempted)
  - Anvil forks from real chain RPC (has real state, not standalone)
  - Local dev by default in all environments (local, container, Cloud Run)
  - User confirmation required for mainnet deployments
  - Chain keywords defined in per-agent SKILLS.md

### 2026-02-18 (session 17)
- **Migrated Midnight MCP from SDK to Toolkit CLI:**
  - **Goal:** Simplify architecture by using official `midnight-node-toolkit` binary instead of ~15 SDK packages
  - **Key Insight:** Node and toolkit are released together with matching versions (0.20.1)
- **Phase 1-3: Core Changes**
  - Added `MIDNIGHT_VERSION = '0.20.1'` constant in config.ts as single source of truth
  - Created `toolkit-client.ts` wrapper for CLI operations (run, version, wallet, deploy, call, state)
  - Rewrote Containerfile to copy toolkit binary from official Docker image
- **Phase 4: Rewrote All MCP Tools**
  - `wallet.ts` - Uses `show-address` and `show-wallet` commands
  - `compile.ts` - Uses toolkit's built-in `compactc compile`
  - `contract-deploy.ts` - Uses `generate-txs contract-simple deploy`
  - `contract-call.ts` - Uses `generate-txs contract-simple call`
  - `query-state.ts` - Uses `contract-state` with fallback to Indexer GraphQL
- **Phase 5: Removed SDK Dependencies**
  - Deleted `providers.ts` (337 lines of SDK integration code)
  - Removed 14 `@midnight-ntwrk/*` packages from package.json
  - Removed `ws` and `rxjs` dependencies (only used by SDK)
  - New deps: `@modelcontextprotocol/sdk`, `zod` (was ~15 packages before)
- **Phase 6: Updated compose.yaml**
  - Added version documentation comments
  - Aligned indexer version to 0.20.1 (matches node/toolkit)
- **Phase 7: Verified Build**
  - TypeScript typecheck: PASSED (22 tests)
  - Docker build: PASSED (2-stage build with toolkit binary)
  - Container test: PASSED (health endpoint, toolkit version verified)
- **Architecture Change:**
  ```
  Before (SDK-based):
  ┌─────────────────────────────────────────────────────────────────┐
  │                    midnight-mcp                                  │
  │  ┌──────────────┐  ┌───────────────────────────────────────┐   │
  │  │ MCP Tools    │──│ providers.ts (SDK integration)        │   │
  │  │ (wallet,     │  │  - WalletBuilder                      │   │
  │  │  deploy,     │  │  - createContractProviders            │   │
  │  │  call)       │  │  - 14 @midnight-ntwrk/* packages      │   │
  │  └──────────────┘  └───────────────────────────────────────┘   │
  └─────────────────────────────────────────────────────────────────┘

  After (Toolkit-based):
  ┌─────────────────────────────────────────────────────────────────┐
  │                    midnight-mcp                                  │
  │  ┌──────────────┐  ┌───────────────────────────────────────┐   │
  │  │ MCP Tools    │──│ toolkit-client.ts (CLI wrapper)       │   │
  │  │ (wallet,     │  │  - execFile('/midnight-node-toolkit') │   │
  │  │  deploy,     │  │  - No SDK dependencies                │   │
  │  │  call)       │  └───────────────────────────────────────┘   │
  │  └──────────────┘                                               │
  │                    ┌───────────────────────────────────────┐   │
  │                    │ /usr/local/bin/midnight-node-toolkit  │   │
  │                    │ (82MB static binary from official img)│   │
  │                    └───────────────────────────────────────┘   │
  └─────────────────────────────────────────────────────────────────┘
  ```
- **Files Modified:**
  - `packages/midnight-mcp/src/config.ts` - Added version, fixed getNodeRpcUrl
  - `packages/midnight-mcp/src/toolkit-client.ts` - NEW (523 lines)
  - `packages/midnight-mcp/src/tools/wallet.ts` - Rewrote for toolkit
  - `packages/midnight-mcp/src/tools/compile.ts` - Rewrote for toolkit
  - `packages/midnight-mcp/src/tools/contract-deploy.ts` - Rewrote for toolkit
  - `packages/midnight-mcp/src/tools/contract-call.ts` - Rewrote for toolkit
  - `packages/midnight-mcp/src/tools/query-state.ts` - Rewrote for toolkit
  - `packages/midnight-mcp/package.json` - Removed 14 SDK deps
  - `packages/midnight-mcp/Containerfile` - Rewrote for npm + toolkit
  - `packages/midnight-mcp/tsconfig.json` - Exclude test files
  - `compose.yaml` - Version comments, indexer version fix
- **Files Deleted:**
  - `packages/midnight-mcp/src/providers.ts` - 337 lines (no longer needed)
- **Limitation:** MVP uses toolkit's built-in `contract-simple` commands
  - Custom contracts with witnesses require toolkit-js (not in Docker image)
  - Full custom contract support is a future enhancement

### 2026-02-18 (session 16)
- **Implemented Midnight Deploy Auto-Funding:**
  - Investigated why Midnight deployment was not completing (compilation worked, deployment failed)
  - Root cause: Wallets on testnets (preview/preprod) are created with random seeds and 0 tDUST balance
  - Solution: Auto-fund wallet from faucet before deployment
- **Changes to `packages/midnight-agent/src/mcp-client.ts`:**
  - Added `FaucetResult` interface for faucet tool responses
  - Added `requestTokens(address, network)` method to call `request_tokens` MCP tool
  - Detects when manual faucet interaction is required (captcha) and extracts faucet URL
- **Changes to `packages/midnight-agent/src/skills/deploy.ts`:**
  - Added auto-funding constants: `MIN_BALANCE_TDUST = 10`, `FUNDING_WAIT_MS = 30000`, `BALANCE_CHECK_INTERVAL_MS = 5000`
  - Added `waitForFunding()` helper that polls wallet balance until funded or timeout
  - Deploy flow now: create wallet → check balance → request faucet if needed → wait for funding → deploy
  - Handles edge cases: local network (skip funding), faucet requires captcha (return manual instructions), timeout (clear error)
- **Updated Deploy Flow:**
  ```
  1. Analyze request, detect network
  2. Get compiled artifacts from session
  3. Create wallet via MCP
  4. [NEW] Check balance and auto-fund if needed:
     - Local network: Skip (genesis wallet pre-funded)
     - Testnet + balance < 10 tDUST: Request from faucet
     - Wait for funding confirmation (max 30s)
     - If faucet requires captcha: Return manual instructions
  5. Deploy contract via MCP
  6. Update session with contract address
  7. Return deployment result
  ```
- **TypeScript compilation passes for both packages**

### 2026-02-18 (session 15)
- **Successfully Deployed All Infrastructure to Cloud Run:**
  - All 6 services deployed and healthy
  - Build time: ~5 minutes (parallel image builds)
  - midnight-mcp with Compact compiler working in production
- **Service URLs:**
  - agent-registry: https://agent-registry-2rejqy6csq-uc.a.run.app
  - somnia-agent: https://somnia-agent-2rejqy6csq-uc.a.run.app
  - midnight-agent: https://midnight-agent-2rejqy6csq-uc.a.run.app
  - midnight-mcp: https://midnight-mcp-2rejqy6csq-uc.a.run.app
  - opencode-login: https://opencode-login-2rejqy6csq-uc.a.run.app
  - opencode-web: https://opencode-web-2rejqy6csq-uc.a.run.app (IAP protected)
- **Verified MCP Integration in Production:**
  - midnight-agent connects to midnight-mcp via SSE in Cloud Run
  - Compact compilation working: `pragma language_version 0.21;` compiles successfully
  - Artifacts generated: zkir, prover/verifier keys, JS contract code
- **Test Results:**
  - All health checks: PASSED
  - Agent registry: PASSED (returns both agents with Cloud Run URLs)
  - Midnight compile: PASSED (via MCP over HTTP)
  - Somnia solidity-gen: LLM called but response parsing needs fix
- **Architecture Validated:**
  ```
  Cloud Run:
  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
  │ agent-registry  │  │  somnia-agent   │  │ midnight-agent  │
  │ (public)        │  │ (public)        │  │ (public)        │
  └─────────────────┘  └─────────────────┘  └────────┬────────┘
                                                     │ MCP/SSE
                                                     ▼
  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
  │ opencode-login  │  │  opencode-web   │  │  midnight-mcp   │
  │ (public)        │  │ (IAP protected) │  │ (public)        │
  └─────────────────┘  └─────────────────┘  └─────────────────┘
  ```
- **Note:** Using Midnight Preview testnet endpoints - no local devnet needed in Cloud Run

### 2026-02-17 (session 14)
- **Completed MCP-via-HTTP Architectural Refactor:**
  - midnight-agent now calls midnight-mcp via HTTP/SSE (MCP protocol) instead of direct imports
  - Only midnight-mcp container has Compact compiler and Midnight.js SDK
  - Compiled artifacts passed via session context (not filesystem paths)
- **Updated Language Version to 0.21:**
  - The Compact compiler v0.28.0 requires `pragma language_version 0.21;` (not 0.20)
  - Updated SKILLS.md (6 code examples + version table)
  - Updated compact-gen.ts fallback prompt
  - Updated config.toml compatibility matrix
- **Test Results Summary:**
  - Agent health check: PASSED
  - compact-gen skill (generate contract): PASSED
  - compile skill (via MCP): PASSED (12 artifacts returned)
  - deploy/call/query-state: Need session continuity (OpenCode web UI)
- **Key Files Modified:**
  - `packages/midnight-agent/SKILLS.md` - Updated to pragma 0.21
  - `packages/midnight-agent/src/skills/compact-gen.ts` - Updated to pragma 0.21
  - `config.toml` - `compact_language = "0.21"`
- **Architecture Note:**
  ```
  midnight-agent (port 4003)
       │
       │ MCP over HTTP/SSE
       ▼
  midnight-mcp (port 4010)
       │
       ├── Compact compiler
       ├── Midnight.js SDK
       └── MCP tools (compile, deploy, call, query)
  ```

### 2026-02-17 (session 13)
- **Implemented Midnight Agent Deploy & Call Skills:**
  - Added `@coding-labs/midnight-mcp` as workspace dependency to midnight-agent
  - Added proper ESM `exports` field to midnight-mcp package.json for submodule imports
  - Created `SessionContext` interface to track state between skill invocations:
    - `compiledDir`: Path to compiled artifacts (from compile step)
    - `contractName`: Contract name extracted from source
    - `contractAddress`: Deployed contract address
    - `network`: Target network (preview/preprod/local)
  - Added `session-update` event type for skills to persist state
  - Updated `MidnightAgentExecutor` to maintain session state per conversation (keyed by contextId)
- **Updated compile.ts Skill:**
  - Extracts contract name from Compact source using regex
  - Emits `session-update` with `compiledDir` and `contractName`
  - No longer cleans up temp directory on success (artifacts needed for deployment)
- **Rewrote deploy.ts Skill (Full Implementation):**
  - Uses MCP tools: `deployContract`, `createWallet`, `loadConfig`
  - Gets `compiledDir` from session context, message path, or embedded artifacts
  - Supports embedded artifacts: extracts from message, writes to temp dir
  - Auto-creates wallet for deployment
  - Emits `session-update` with `contractAddress` and `network`
  - Returns deployment result with address, txId, block height
- **Rewrote call.ts Skill (Full Implementation):**
  - Uses MCP tools: `callContract`, `createWallet`, `loadConfig`
  - Extracts circuit name, contract address, and arguments from message
  - Uses session context for `compiledDir`, `contractAddress`, `contractName`
  - Returns call result with txId, txHash, block height
- **Architecture:**
  ```
  User: "Compile this contract" → compile skill
           │
           ├── Emits: session-update { compiledDir, contractName }
           │
  User: "Deploy it to preprod" → deploy skill
           │
           ├── Uses session.compiledDir, session.contractName
           ├── Creates wallet via MCP
           ├── Deploys via MCP
           ├── Emits: session-update { contractAddress, network }
           │
  User: "Call the increment circuit" → call skill
           │
           ├── Uses session.compiledDir, session.contractAddress
           ├── Executes circuit via MCP
           └── Returns transaction result
  ```
- **Testing:** Both packages pass TypeScript compilation and type checking

### 2026-02-17 (session 12)
- **Investigated Local Midnight Devnet:**
  - Discovered `CFG_PRESET=dev` flag on `midnightntwrk/midnight-node` enables standalone mode
  - Standalone mode runs an isolated chain -- no Cardano dependency, no external network
  - Genesis-minted tokens available immediately -- no faucet needed
  - Reference: Midnight's official `example-bboard` repo uses this pattern
  - Source: `midnightntwrk/midnight-node` repo `local-environment/` folder + Midnight docs
- **Added Midnight Local Devnet to compose.yaml (profile: `midnight-local`):**
  - `midnight-node-dev`: `midnightntwrk/midnight-node:0.20.1` (standalone, port 9944)
  - `midnight-indexer-dev`: `midnightntwrk/indexer-standalone:3.0.0` (port 8088)
  - `midnight-proof-server`: `midnightnetwork/proof-server:7.0.0-rc.1` (port 6300, was `:latest`)
  - All versions pinned to compatibility matrix v1.0 (Ledger 7.0.0)
  - Usage: `docker compose --profile midnight-local up -d`
- **Updated midnight-mcp Config:**
  - Added `local` network to `NETWORKS` (node ws://localhost:9944, indexer http://localhost:8088, proof http://localhost:6300)
  - Fixed `DOCKER_IMAGES` to derive versions from `COMPATIBILITY_MATRIX` (node 0.20.0→0.20.1, proof-server latest→7.0.0-rc.1)
  - Widened `defaultNetwork` type to include `'local'`
  - Updated docker-images resource with standalone devnet usage instructions
- **Updated config.toml:**
  - Added `midnight-local` network section with localhost endpoints
  - Added `node = "0.20.1"` to compatibility section (was missing)
- **Tests:** 22 passing (was 20, added 2 for local network + version alignment)
- **Added midnight-mcp to Cloud Run Deployment:**
  - `cloudbuild.yaml`: Added `build-midnight-mcp`, `push-midnight-mcp`, `deploy-midnight-mcp`, `get-midnight-mcp-url` steps
  - midnight-agent deploy now receives `MIDNIGHT_MCP_URL` env var
  - `scripts/cloud.sh`: Added `midnight-mcp` to `cmd_urls()` and `cmd_delete()`
  - Deployment order: registry → midnight-mcp → somnia-agent → midnight-agent → login → opencode-web
  - Cloud Run config: 512Mi, 1 CPU, 0-3 instances, port 8080, `MIDNIGHT_DEFAULT_NETWORK=preview`
- **Updated container.sh:**
  - `cmd_compose_up/down/build` now pass `--profile midnight-local` by default
  - `make up` / `make rebuild` starts the full Midnight devnet alongside our agents
- **Outstanding TODOs (Midnight deploy pipeline):**
  - Deploy skill not yet functional -- needs compiled artifacts, Midnight.js SDK, wallet connection, proof server
  - Current state: compact-gen (generates code) and compile (via MCP) work, but deploy/call/query-state need Midnight.js integration
  - Wallet: Need headless wallet for agent use (Lace is browser-only)
  - Decision needed: deploy midnight infra (node/indexer/proof-server) to Cloud Run, or use remote Preview endpoints in prod?

### 2026-02-14 (session 11)
- **Created midnight-mcp Package (`packages/midnight-mcp/`):**
  - Combined MCP server for Midnight Network infrastructure
  - SSE transport over HTTP on port 4010
  - 6 tools: `compact_compile`, `compact_validate`, `query_contract_state`, `query_blocks`, `zk_prove`, `request_tokens`
  - 3 resources: `midnight://networks`, `midnight://compatibility`, `midnight://docker-images`
  - 20 tests passing
- **Migrated Both Agents from Genkit to AI SDK:**
  - **somnia-agent:** Switched from `genkit` + `@genkit-ai/vertexai` to `ai@^5.0` + `@ai-sdk/google-vertex@^3.0`
  - **midnight-agent:** Same migration as somnia-agent
  - Both agents now use Claude Opus 4.5 via Vertex AI (`claude-opus-4-5` model ID)
  - 7 source files rewritten (2x genkit.ts, 5x skill files)
- **Bug Fixes:**
  - **Model ID:** `claude-opus-4.5` → `claude-sonnet-4@20250514` → `claude-opus-4-5` (correct Vertex AI format)
  - **Compile command:** `compact build ... --output ...` → `compact compile ... ...` (3 files fixed)
  - **Containerfile:** Added `unzip`, fixed PATH to `/root/.local/bin`, `compact update` now works
  - **compose.yaml:** Added `env_file`, hardcoded healthcheck port for midnight-mcp
  - **Skill routing:** Rewrote `detectSkill()` in both agents -- generation-first priority, tighter patterns
  - **Code extraction:** compact-gen now handles multiple fence formats + pragma-based fallback

### 2026-02-13 (session 10 continued)
- **Fixed cloud.sh Missing Services:**
  - Added `midnight-agent` and `opencode-login` to `cmd_urls()` function
  - Added `midnight-agent` and `opencode-login` to `cmd_delete()` function
  - Now `make cloud-urls` shows all deployed services
- **Added Wallet Icon to UI Library:**
  - Added new `wallet` icon SVG to `opencode/packages/ui/src/components/icon.tsx`
  - Standard wallet shape (rectangular with fold)
- **Improved Wallet Button UX:**
  - Changed icon from `link` to `wallet` in wallet-button.tsx
  - Changed button label from "Connect" to "Connect Wallet" (clearer intent)
- **Added Titlebar Button Spacing:**
  - Wrapped AuthButton and WalletButton in flex container with `gap-3`
  - Provides consistent spacing between auth and wallet buttons
- **Commits:**
  - `7950ad64d` (opencode): feat(ui): add wallet icon and improve wallet button UX
  - `42f846eb` (coding-labs): fix: add midnight-agent and opencode-login to cloud.sh, update opencode submodule

### 2026-02-13 (session 10)
- **Updated ASCII Art to "dCODER":**
  - Changed login page logo from "DCODER" (all uppercase) to "dCODER" (lowercase d)
  - Used mixed style: text-style "d" + block-style "CODER" for visual distinction
  - File: `packages/opencode-login/public/index.html`
- **Fixed Local Frontend Serving:**
  - **Root Cause:** `bun run dev -- web` was proxying frontend to `app.opencode.ai` when `OPENCODE_STATIC_DIR` not set
  - This caused custom wallet/auth buttons to be missing (production OpenCode doesn't have them)
  - **Solution:** Updated `dev.sh` to run Vite dev server alongside backend
- **Updated dev.sh with 4 Run Modes:**
  - `run-all` - Vite dev server (hot reload) - access at http://localhost:3000
  - `run-all-built` - Pre-built frontend (production-like) - access at http://localhost:4097
  - `run-all-with-login` - Vite + login page - access at http://localhost:4098
  - `run-all-with-login-built` - Pre-built + login page
- **Removed Proxy Fallback to app.opencode.ai:**
  - **Breaking Change** in opencode submodule
  - Server now returns 404 with helpful message when frontend not configured
  - Message: "Frontend not configured. Set OPENCODE_STATIC_DIR or run Vite dev server."
  - Removed unused `proxy` import from `hono/proxy`
  - File: `opencode/packages/opencode/src/server/server.ts`
- **Commits:**
  - `b75b4025` (coding-labs): feat: update login ASCII art to dCODER and fix local frontend serving
  - `af29c73d2` (opencode): fix!: remove proxy fallback to app.opencode.ai
  - `62456d68` (coding-labs): chore: update opencode submodule (remove proxy fallback)

### 2026-02-13 (session 9)
- **Implemented dCoder Login Page:**
  - Created `packages/opencode-login/` - Landing page service with "Sign in with Google"
  - Files: package.json, server.js, Containerfile, README.md, .npmrc
  - Public files: index.html (ASCII art dCoder logo), styles.css (dark theme, orange accents)
  - Express static server on port 8080, serves `/health`, `/config`, `/` endpoints
- **Login Page Features:**
  - Bold block ASCII art "dCoder" logo with orange glow animation
  - Tagline: "Your AI Coding Companion for Web3"
  - 4 feature bullet points
  - "Sign in with Google" button (orange themed) links to IAP-protected app
  - Responsive design, dark theme, accessibility support (reduced motion)
- **Updated Cloud Build (`cloudbuild.yaml`):**
  - Added `build-login`, `push-login` steps for container build
  - Added `deploy-login` step - deploys to Cloud Run (public, no IAP)
  - Added `get-login-url` step to capture login page URL
  - Added `update-login` step to set `APP_URL` env var (opencode-web URL)
  - Updated `deploy-opencode` to include `LOGIN_PAGE_URL` env var
  - Updated `print-urls` to show login page URL
- **Updated compose.yaml:**
  - Added `opencode-login` service with `login` profile (optional)
  - Usage: `docker compose --profile login up -d`
  - Login page runs on port 4098
- **Updated Backend (`global.ts`):**
  - Added `/global/app-config` endpoint returning `{ loginPageUrl: string | null }`
  - Reads from `LOGIN_PAGE_URL` environment variable
- **Updated Frontend (`auth.tsx`):**
  - Fetches `loginPageUrl` from `/global/app-config` on mount
  - `signOut()` now redirects to login page if configured (production)
  - In local dev, just clears state (gcloud auth remains)
- **Updated dev.sh:**
  - Added `run-all-with-login` command to start all services including login page
  - Login page runs on port 4098, main app on port 4097
- **Architecture:**
  ```
  Production:
  ┌─────────────────┐         ┌─────────────────┐
  │ opencode-login  │  ──→    │  opencode-web   │
  │ (Cloud Run)     │  click  │ (Cloud Run+IAP) │
  │ Public          │         │ IAP Protected   │
  └─────────────────┘         └─────────────────┘

  Local Dev:
  ┌─────────────────┐         ┌─────────────────┐
  │ localhost:4098  │  ──→    │ localhost:4097  │
  │ (optional)      │         │ (gcloud auth)   │
  └─────────────────┘         └─────────────────┘
  ```
- **Tested locally:**
  - Server starts correctly on port 8080
  - `/health` returns `{ status: "ok" }`
  - `/config` returns `{ appUrl: "..." }`
  - HTML page renders with ASCII art and features

### 2026-02-13 (session 8)
- **Completed Midnight Agent Implementation:**
  - Created `packages/midnight-agent/` with full A2A server structure
  - Files created: package.json, tsconfig.json, Containerfile
  - Source files: index.ts, agent-card.ts, executor.ts, genkit.ts
  - Skills: skills/index.ts, skills/compact-gen.ts
  - Documentation: SKILLS.md (~400 lines Compact language reference), MIDNIGHT-AGENT.md
- **Updated Configuration Files:**
  - `config.toml` - Added midnight agent with enabled=true, port 4003, network endpoints (Preview/Preprod)
  - `compose.yaml` - Added midnight-agent service block with health check
- **Updated Scripts:**
  - `scripts/common.sh` - Added MIDNIGHT_AGENT_PORT, get_agent_url(), list_agents()
  - `scripts/dev.sh` - Added midnight-agent to build, run-all, typecheck, clean, clean-all
  - `scripts/a2a.sh` - Added AGENT env var support for targeting agents
- **Created ADDING-NEW-AGENT.md:**
  - Comprehensive step-by-step guide for adding new agents
  - Covers all files needed, code templates, configuration updates
  - File checklist and port allocation table
- **Updated README.md:**
  - Updated architecture diagram (Midnight now active, Sonic future)
  - Updated agent table (Midnight Active)
  - Updated port tables to include midnight-agent
  - Added reference to ADDING-NEW-AGENT.md guide
- **Midnight Agent Technical Details:**
  - Port: 4003
  - No viem - Midnight is NOT EVM-compatible
  - Uses Compact language (pragma 0.20) instead of Solidity
  - Active skill: compact-gen (LLM generates Compact code)
  - Planned skills: compile, deploy, call, query-state, private-state
- **Version Compatibility Matrix (from Midnight docs):**
  - Compact Compiler: 0.28.0
  - Compact Language: 0.20 (pragma)
  - Compact Runtime: 0.14.0
  - Midnight.js: v3.0.0
  - Ledger: 7.0.0
  - Proof Server: 7.0.0
  - Indexer: v3.0.0
  - Environments: Preview, Preprod (NOT Testnet_02 which is outdated)
- **Blocked:** pnpm install failed due to network access restrictions (gpkg/Corp Airlock)
  - Build and test deferred to next session when network is available

### 2026-02-12 (continued - session 7)
- **Researched Midnight Network for agent implementation:**
  - Midnight is privacy-first blockchain using ZK proofs (NOT EVM-compatible)
  - Smart contracts written in Compact language (purpose-built DSL)
  - Uses Midnight.js SDK (TypeScript) instead of viem/ethers.js
  - Requires Proof Server (Docker) for local ZK proof generation
  - Wallet: Lace Midnight Preview (Chrome extension, different from MetaMask)
  - Network: Testnet_02 with tDUST token
- **Documented compatibility matrix:**
  - Compactc v0.26.0, Midnight.js v2.1.0, Proof Server v4.0.0
  - Node.js 20.x required, Midnight Indexer v2.1.4 for state queries
- **Identified key components for midnight-agent:**
  - Compact compiler (install via curl script)
  - Midnight.js SDK packages (@midnight-ntwrk/*)
  - Proof Server sidecar (Docker container on port 6300)
  - Indexer client for GraphQL state queries
- **Planned skill set:**
  - compact-gen, compile, deploy, call, query-state, private-state
- **Updated .context.md with full Midnight Network documentation**

### 2026-02-12 (continued - session 6)
- **Implemented Google Authentication via IAP:**
  - Added `[default.auth]` section to config.toml with `allowed_users` array
  - Created `AuthConfig` type in shared config package
  - Added `sync-users` command to `scripts/cloud.sh` to sync config.toml users to IAP
  - Updated `cloudbuild.yaml` to enable IAP on opencode-web (`--iap` flag)
  - Added `setup-iap` step to grant IAP service agent `run.invoker` role
  - Added `/global/me` endpoint to OpenCode server (returns IAP user or gcloud account)
  - Created `AuthProvider` and `AuthButton` components for frontend
  - Auth button shows next to wallet button in titlebar
- **User Management Workflow:**
  - Edit `config.toml` → `[default.auth.allowed_users]`
  - Run `make cloud-sync-users` to sync to IAP
  - Users added/removed without code deployment
- **Local Development:**
  - `/global/me` endpoint uses `gcloud config get account` when IAP headers not present
  - Developer sees their real Google account in the UI

### 2026-02-12 (continued - session 5)
- **Fixed Cloud Build Pipeline (`cloudbuild.yaml`):**
  - Changed `SHORT_SHA` → `_TAG` substitution (SHORT_SHA only works with repo triggers, not manual builds)
  - Removed `PORT` from `--set-env-vars` (Cloud Run sets this automatically, was causing deploy failure)
  - Added `update-registry` step to pass `SOMNIA_AGENT_URL` to agent-registry after somnia-agent deploys
  - Added `GOOGLE_CLOUD_PROJECT` and `GOOGLE_CLOUD_LOCATION` env vars to opencode-web deploy
- **Fixed IAM Permissions (`scripts/cloud.sh`):**
  - Cloud Build uses default Compute SA for gcloud steps (not Cloud Build SA)
  - Added `roles/run.admin` and `roles/iam.serviceAccountUser` to Compute SA in setup
- **Fixed agent-registry Config (`packages/agent-registry/Containerfile`):**
  - Added `COPY config.toml ./` to bake config into the image
  - Config is required for agent-registry to know which agents exist
- **All Cloud Run Services Deployed and Healthy:**
  - agent-registry: https://agent-registry-2rejqy6csq-uc.a.run.app
  - somnia-agent: https://somnia-agent-2rejqy6csq-uc.a.run.app
  - opencode-web: https://opencode-web-2rejqy6csq-uc.a.run.app
- **Vertex AI Providers Working:**
  - `GOOGLE_CLOUD_PROJECT=kunal-scratch` and `GOOGLE_CLOUD_LOCATION=global` passed to opencode-web
  - Both `google-vertex` and `google-vertex-anthropic` providers now autoload

### 2026-02-12 (continued - session 4)
- **Simplified Makefile (209 → 103 lines):**
  - Reorganized around 3 runtimes: local, container, cloud
  - Reduced from 45+ targets to 20 targets
  - Added `concurrently` dependency for parallel service execution
  - New `make run` - starts all services (registry + agent + opencode) in one command
  - Simplified aliases: `up`, `down`, `logs`, `rebuild`
  - Removed: test-*, opencode-*, agent-card, skills, shell, etc.
- **Updated scripts/dev.sh:**
  - Added `run-all` command using `concurrently`
  - All services start with prefixed, color-coded output
  - Clean shutdown on Ctrl+C with `--kill-others-on-fail`
- **Fixed LLM Location Bug in generate-env.ts:**
  - Bug: `GOOGLE_CLOUD_LOCATION` was set from default provider (vertex-gemini → us-central1)
  - But OpenCode uses vertex-anthropic which requires `global` location
  - Fix: `GOOGLE_CLOUD_LOCATION` now derived from component's assigned provider
  - OpenCode reads `GOOGLE_CLOUD_LOCATION` first, so this was causing wrong region errors
- **Updated Documentation:**
  - Simplified README.md to match new Makefile structure
  - Removed verbose target tables, consolidated sections

### 2026-02-12 (continued - session 3)
- **Added Container Build Targets:**
  - Added `compose-build` command to `scripts/container.sh`
  - Added Makefile targets: `compose-build`, `compose-rebuild`
  - `compose-rebuild` = build + start (use after code changes)
  - `compose-up` = start only (quick restart, no rebuild)
- **Fixed Duplicate Compose File:**
  - Removed old `docker-compose.yml` which was conflicting with `compose.yaml`
  - Podman-compose was merging both files, causing duplicate services
  - The `opencode` service from old file was overriding `opencode-web` port mapping
- **Fixed LLM Model Configuration:**
  - Changed `gemini-3.0-pro` → `gemini-2.0-flash` (valid model)
  - Changed `claude-opus-4.5` → `claude-sonnet-4` (valid model)
  - Changed Anthropic location from `global` → `us-east5` (Claude requires specific regions)
- **Verified Full Container Stack:**
  - All three containers healthy: agent-registry (4000), somnia-agent (4001), opencode-web (3000→8080)
  - Tested A2A skill: `./scripts/a2a.sh send "Create a simple counter contract"` - SUCCESS
- **Updated Documentation:**
  - Added container workflow section to `.context.md`
  - Updated README.md with new container targets and verification steps

### 2026-02-12 (continued - session 2)
- **Fixed GCloud Credentials in Containers:**
  - Changed Vertex AI location from `global` to `us-central1` in config.toml and genkit.ts
  - The compose.yaml already had ADC mount configured correctly:
    - `volumes: ${HOME}/.config/gcloud:/creds:ro`
    - `GOOGLE_APPLICATION_CREDENTIALS=/creds/application_default_credentials.json`
  - All LLM-dependent skills now work in containers
  - Tested: solidity-gen, query-state skills working correctly
  - Updated README.md with new skills table and container setup instructions
- **Fixed opencode-web Container:**
  - Fixed port mismatch: container runs on 8080 (Containerfile.web), was mapped to 3000:3000
  - Changed to `3000:8080` mapping (host:container)
  - Added GCloud credentials mount for Vertex AI support
  - Added healthcheck matching Containerfile.web
  - Added explanatory comments about combined frontend+backend in containers
  - All three containers now healthy:
    - agent-registry (port 4000)
    - somnia-agent (port 4001)
    - opencode-web (port 3000 -> 8080)

### 2026-02-12 (continued - session 1)
- **Phase 2 Skills Implemented:**
  - Created `deploy.ts` skill - prepares unsigned deployment transactions with viem
  - Created `tx-status.ts` skill - checks transaction status on Somnia chains
  - Created `query-state.ts` skill - reads contract state and balances
  - All skills use viem for RPC calls
  - Skills emit artifacts (JSON, text) for results
  - Updated skills/index.ts to register new handlers
  - All typechecks pass

### 2026-02-12
- **Agent Registry Implementation:**
  - Designed A2A discovery approach (static registry with admin-managed config)
  - Created centralized `config.toml` at project root
  - Created shared config loader (`packages/shared/src/config/`)
  - Created `scripts/generate-env.ts` to generate `.env.generated` for containers
  - Created `agent-registry` package (Hono HTTP server, reads config.toml)
  - Updated `somnia-agent` to use shared config loader
  - Updated `compose.yaml` to use `env_file` and container URL overrides
  - Updated OpenCode A2A plugin to fetch agents from registry
  - Added caching with TTL and fallback to static config
  - Updated Makefile with `registry-dev`, `generate-config` targets
  - All typechecks pass

### 2026-02-11
- Initial project discussion and architecture design
- Researched A2A protocol and TypeScript SDK
- Researched Somnia blockchain (performance, gas model, unique features)
- Decided on tech stack: TypeScript, pnpm, Vertex AI, Podman
- Designed agent skills (6 skills for Somnia agent)
- Created SKILLS.md approach for RAG/LLM context
- Completed Phase 1 implementation:
  - Created monorepo with pnpm workspaces
  - Built shared package with types for wallet, LLM, and network configs
  - Built somnia-agent with A2A SDK integration
  - Implemented solidity-gen skill with Genkit/Vertex AI
  - Created Containerfile and compose.yaml for Podman
  - All typechecks pass, builds successfully
- Added development tooling:
  - Makefile with all common operations
  - Modular shell scripts in scripts/ (common, dev, test, container, a2a)
  - Vitest for unit testing (10 tests passing)
  - ESLint + Prettier for linting and formatting
  - README.md with Quick Start guide
- **OpenCode Integration (Phase 1.5):**
  - Cloned OpenCode v1.1.56 to `opencode/` directory
  - Created feature branch: `feature/a2a-wallet-integration`
  - Added wallet connection UI:
    - `opencode/packages/app/src/context/wallet.tsx` - SolidJS wallet context
    - `opencode/packages/app/src/components/wallet-button.tsx` - Wallet button component
    - Modified `app.tsx` to integrate wallet provider
  - Added A2A plugin (baked into OpenCode):
    - `opencode/packages/opencode/src/plugin/a2a/agents.ts` - Agent registry
    - `opencode/packages/opencode/src/plugin/a2a/router.ts` - Intent detection & routing
    - `opencode/packages/opencode/src/plugin/a2a/client.ts` - A2A SDK wrapper
    - `opencode/packages/opencode/src/plugin/a2a/index.ts` - Plugin with 3 custom tools
  - Added to INTERNAL_PLUGINS in `opencode/packages/opencode/src/plugin/index.ts`
  - Created `opencode/Containerfile.web` for container build
  - Updated compose.yaml with opencode-web service
  - Updated Makefile with opencode-dev, opencode-typecheck, compose-up, compose-down
  - All typechecks pass in OpenCode fork
- **Frontend Configuration (config.toml):**
  - Added `opencode/packages/app/config.toml` for server configuration
  - Created `src/lib/config.ts` loader with env var override support
  - Updated Makefile: opencode-server on port 4097, CLI override support
  - Config resolution: default → env section → CLI env vars
  - Fixed A2A tools issue by separating server ports (4097 vs default 4096)

## OpenCode Fork Details

The OpenCode fork lives in `opencode/` directory (added to .gitignore until GitHub fork is created).

**Repo Structure:**
```
opencode/
├── packages/
│   ├── opencode/src/plugin/a2a/   # A2A plugin (NEW)
│   │   ├── agents.ts              # Agent registry
│   │   ├── router.ts              # Intent routing
│   │   ├── client.ts              # A2A SDK wrapper
│   │   └── index.ts               # Plugin entry point
│   ├── app/                       # Web frontend (SolidJS + Vite)
│   │   ├── config.toml            # Frontend config (NEW)
│   │   ├── vite.config.ts         # Modified to load config.toml
│   │   └── src/
│   │       ├── lib/config.ts      # Config loader with env override (NEW)
│   │       ├── context/wallet.tsx # Wallet context (NEW)
│   │       ├── components/
│   │       │   └── wallet-button.tsx  # Wallet button (NEW)
│   │       └── app.tsx            # Modified to use config + WalletProvider
│   └── ...
├── Containerfile.web              # Container build (NEW)
└── ...
```

**Remotes (configured, pending fork creation):**
- `origin`: git@github.com:dcentral-labs/opencode.git (fork - needs to be created)
- `upstream`: https://github.com/anomalyco/opencode.git (original)

**Branch:** `feature/a2a-wallet-integration`

**A2A Plugin Tools:**
1. `a2a-send` - Send message to blockchain agent (auto-routes or explicit)
2. `a2a-agents` - List available agents and their status
3. `a2a-capabilities` - Get agent card/skills

**Wallet Features:**
- MetaMask/injected provider connection
- Network switching (Somnia Mainnet 5031, Testnet 50312)
- Transaction signing via wallet
- Header integration with address display

## Local Integration Testing

### Prerequisites
```bash
# 1. Install dependencies
make install

# 2. Ensure GCloud credentials are configured (for Vertex AI)
gcloud auth application-default login
```

### Running Locally (Single Command)

```bash
# Start all services in one terminal
make run
```

This starts all 3 services with color-coded, prefixed output:
- **[registry]** Agent Registry on :4000
- **[agent]** Somnia Agent on :4001
- **[opencode]** OpenCode backend on :4097

Then open http://localhost:3000 in your browser.

Press Ctrl+C to stop all services.

### Verification Steps

```bash
# Check all services are healthy
make health

# Send A2A message
make send MSG="Create an ERC-20 token"
```

## Commands to Create Fork

After session, run:
```bash
# 1. Create fork on GitHub
gh repo fork anomalyco/opencode --org dcentral-labs --clone=false

# 2. In the opencode directory, push the feature branch
cd /home/kunall/scratchpad/dcentral-labs/onchain-agents/coding-labs/opencode
git push -u origin feature/a2a-wallet-integration

# 3. Later, to convert to submodule (optional)
cd /home/kunall/scratchpad/dcentral-labs/onchain-agents/coding-labs
rm -rf opencode
git submodule add -b feature/a2a-wallet-integration git@github.com:dcentral-labs/opencode.git opencode
```

## Midnight Network Info

Midnight is a privacy-first blockchain using zero-knowledge proofs for selective disclosure:
- **Focus:** Privacy-preserving DApps with ZK proofs
- **Smart Contract Language:** Compact (purpose-built DSL for ZK circuits)
- **Network:** Testnet_02 (current testnet environment)
- **Native Token:** tDUST (testnet), DUST (mainnet)
- **Wallet:** Lace Midnight Preview (Chrome extension)
- **Proof System:** Kachina (ZK proving system)

### Key Differences from EVM Chains

| Aspect | EVM (Somnia) | Midnight |
|--------|--------------|----------|
| Contract Language | Solidity | Compact |
| Execution Model | EVM bytecode | ZK circuits |
| State Model | Account-based | UTXO + dual ledger (public/private) |
| Privacy | Public by default | Private by default |
| Tooling | viem, ethers.js | Midnight.js, Compact compiler |

### Midnight Compatibility Matrix (Feb 2026 - Ledger 7.0.0)

| Component | Version | Purpose |
|-----------|---------|---------|
| Environment | Preview, Preprod | Active networks (NOT Testnet_02) |
| Compact Compiler | 0.28.0 | Contract compiler |
| Compact Language | 0.20 | Language pragma version |
| Compact Runtime | 0.14.0 | Runtime library |
| Midnight.js | v3.0.0 | TypeScript SDK |
| Ledger | 7.0.0 | Blockchain ledger |
| Proof Server | 7.0.0 | ZK proof generation |
| Indexer | v3.0.0 | Blockchain indexer |

**Note:** Testnet_02 is outdated. Use Preview or Preprod environments.

**Source:** https://docs.midnight.network/next/relnotes/overview

### Components Needed for Midnight Agent

1. **Compact Compiler** - Generates ZK circuits from `.compact` files
   - Install: `curl --proto '=https' --tlsv1.2 -LsSf https://github.com/midnightntwrk/compact/releases/download/compact-v0.2.0/compact-installer.sh | sh`
   - Usage: `compact compile <contract.compact> <output-dir>`

2. **Midnight.js SDK** - TypeScript SDK for DApp development
   - Package: `@midnight-ntwrk/midnight-js-contracts`
   - Provides: Transaction creation, wallet interaction, state queries, ZK proof generation
   - GitHub: https://github.com/midnightntwrk/midnight-js

3. **Proof Server** (Docker) - Local ZK proof generation
   - Run: `docker run -p 6300:6300 midnightnetwork/proof-server -- midnight-proof-server --network testnet`
   - Required for generating ZK proofs locally

4. **Lace Wallet** - Browser extension for Midnight
   - Chrome extension: Required for transaction signing
   - Testnet faucet: https://midnight.network/test-faucet/

5. **Indexer** - Query blockchain state
   - GraphQL API for querying blocks and contract state
   - Package: `@midnight-ntwrk/midnight-js-indexer-public-data-provider`

### Midnight Agent Skills (Planned)

| Skill | Description | Implementation Notes |
|-------|-------------|---------------------|
| `compact-gen` | Generate Compact contracts | LLM generates `.compact` code |
| `compile` | Compile Compact to ZK circuits | Shell out to `compact compile` |
| `deploy` | Deploy contract to Midnight | Uses Midnight.js + Lace wallet |
| `call` | Call contract circuit | Executes circuits with ZK proofs |
| `query-state` | Read public ledger state | Midnight Indexer GraphQL |
| `private-state` | Manage private state | Local encrypted storage |

### Example Compact Contract

```compact
pragma language_version 0.17;

// Public ledger state
export ledger counter: Uint32;

// Circuit (ZK-proved function)
export circuit increment(): [] {
  counter = counter + 1;
}
```

### Midnight Agent Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    midnight-agent                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Skills                                                 │  │
│  │  - compact-gen (LLM generates Compact code)           │  │
│  │  - compile (shell to compact compiler)                │  │
│  │  - deploy (Midnight.js + wallet)                      │  │
│  │  - call (execute circuit with ZK proof)               │  │
│  │  - query-state (Indexer GraphQL)                      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────┬───────────────────────┘
                                      │
     ┌────────────────────────────────┼────────────────────────┐
     ▼                                ▼                        ▼
┌──────────────┐              ┌──────────────┐         ┌──────────────┐
│ Compact      │              │ Proof Server │         │ Midnight     │
│ Compiler     │              │ (Docker)     │         │ Indexer      │
│              │              │              │         │              │
│ .compact →   │              │ ZK proof     │         │ GraphQL API  │
│ ZK circuits  │              │ generation   │         │ for state    │
└──────────────┘              └──────────────┘         └──────────────┘
```

### Implementation Considerations

1. **No viem/ethers.js** - Midnight is NOT EVM-compatible, uses its own SDK
2. **Proof Server Required** - ZK proofs need local Docker container
3. **Dual Ledger Model** - Public ledger + private ledger (UTXO-based)
4. **Circuit Execution** - Contracts execute locally, produce ZK proofs
5. **Lace Wallet** - Different from MetaMask, Chrome-only currently
6. **TypeScript SDK** - Midnight.js is well-documented and TypeScript-native

### References

- Midnight Docs: https://docs.midnight.network
- Midnight.js GitHub: https://github.com/midnightntwrk/midnight-js
- Compact Language: https://docs.midnight.network/compact
- Testnet Faucet: https://midnight.network/test-faucet/
- Discord: https://discord.com/invite/midnightnetwork

## Known Issues & Investigations

### Issue: A2A Tools Load in TUI but Not Web Frontend (2026-02-11)

**Status:** Fix Implemented - Separate Server Ports

**Symptom:** The A2A plugin tools (`a2a-send`, `a2a-agents`, `a2a-capabilities`) work when using `opencode` TUI but are not available when using the web frontend (`opencode web`).

**Resolution:** The issue was caused by running two OpenCode instances on the same port (4096). The current OpenCode TUI session and the opencode-server for A2A testing were conflicting. 

**Fix:** Configure opencode-server to run on port 4097 (via Makefile) and frontend to connect to 4097 (via config.toml). See "Configuration" section above.

**Root Cause Analysis:**

Both TUI and Web use the same backend server (`packages/opencode/src/server/server.ts`). The key components:

1. **Plugin Registration Path:**
   ```
   server.ts middleware (line 220-237)
     → Instance.provide({ init: InstanceBootstrap })
       → InstanceBootstrap() (bootstrap.ts:17)
         → Plugin.init() (plugin/index.ts:136)
           → loads INTERNAL_PLUGINS including A2APlugin
   ```

2. **Tool Loading Path:**
   ```
   SessionPrompt (prompt.ts:720)
     → ToolRegistry.tools()
       → ToolRegistry.state() (registry.ts:34)
         → Plugin.list() (plugin/index.ts:132)
           → returns hooks from all loaded plugins
   ```

3. **Key Difference Between TUI and Web:**

   | Aspect | TUI (worker.ts) | Web (web.ts) |
   |--------|-----------------|--------------|
   | Bootstrap | Immediate via `startEventStream(process.cwd())` at line 97 | Lazy on first HTTP request |
   | Directory | Uses `process.cwd()` | Uses `x-opencode-directory` header from frontend |
   | Instance State | Cached per directory | Cached per directory |

4. **Potential Issues:**
   - **Directory Mismatch:** Web frontend sends directory via `x-opencode-directory` header (SDK `client.ts:21-28`). If this directory differs from TUI's `process.cwd()`, separate Instance states are created, each loading plugins independently.
   - **Race Condition:** `ToolRegistry.state()` calls `Plugin.list()`. If accessed before `Plugin.init()` completes, plugins may not be registered.
   - **Plugin Error:** If A2A plugin throws during initialization (caught at `plugin/index.ts:54-59`), it won't be registered but error is only logged.

**Investigation Steps:**
1. Check server logs for "loading internal plugin" + "a2a" when web frontend makes requests
2. Verify directory being sent from web frontend matches expected value
3. Call `/experimental/tool/ids` endpoint from both TUI and web to compare registered tools
4. Check for any errors during plugin initialization

**Relevant Files:**
- `opencode/packages/opencode/src/server/server.ts` - Server middleware (line 220-237)
- `opencode/packages/opencode/src/project/bootstrap.ts` - Instance bootstrap
- `opencode/packages/opencode/src/plugin/index.ts` - Plugin loading
- `opencode/packages/opencode/src/tool/registry.ts` - Tool registry
- `opencode/packages/opencode/src/cli/cmd/web.ts` - Web command
- `opencode/packages/opencode/src/cli/cmd/tui/worker.ts` - TUI worker
- `opencode/packages/sdk/js/src/v2/client.ts` - SDK client (directory header)

## References

- A2A Protocol: https://a2a-protocol.org
- A2A JS SDK: https://github.com/a2aproject/a2a-js
- A2A Samples: https://github.com/a2aproject/a2a-samples
- Somnia Docs: https://docs.somnia.network
- Midnight Docs: https://docs.midnight.network
- Midnight.js: https://github.com/midnightntwrk/midnight-js
- Compact Language: https://docs.midnight.network/compact
- Vertex AI: Project `kunal-scratch`, location `us-central1`
- OpenCode: https://github.com/anomalyco/opencode (v1.1.56)

## Container Development Workflow

### Building and Running Containers

```bash
# Build and start all containers:
make rebuild

# Start containers (no rebuild):
make up

# Stop all containers:
make down

# View logs:
make logs
```

### When to Rebuild

| Scenario | Command |
|----------|---------|
| First time setup | `make rebuild` |
| Changed TypeScript code | `make rebuild` |
| Changed Containerfile | `make rebuild` |
| Changed compose.yaml | `make up` (restart only) |
| Changed config.toml | `make up` (env vars regenerated) |

### Verifying Containers

```bash
# Check all services are healthy
make health

# Or individually:
curl http://localhost:4000/health  # agent-registry
curl http://localhost:4001/health  # somnia-agent
curl http://localhost:3000/global/health  # opencode-web

# Test somnia-agent skill
make send MSG="Create a simple counter contract"
```

### Midnight Local Devnet

Run a standalone Midnight blockchain locally for development (no testnet tokens needed):

```bash
# Start local Midnight devnet (3 containers)
docker compose --profile midnight-local up -d

# Verify node is producing blocks
curl -s -X POST http://localhost:9944 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"chain_getHeader","params":[]}' | jq .result.number

# Ports:
#   Node RPC:      http://localhost:9944
#   Indexer:       http://localhost:8088
#   Proof Server:  http://localhost:6300

# Stop (volumes are ephemeral -- all state is lost)
docker compose --profile midnight-local down
```

**How it works:**
- `CFG_PRESET=dev` on the node runs an isolated chain (no Cardano, no external network)
- Genesis-minted tokens are available immediately -- no faucet required
- The standalone indexer watches the local node and provides GraphQL for state queries
- Set `MIDNIGHT_DEFAULT_NETWORK=local` to point midnight-mcp tools at the local chain

**Versions:** All images pinned to Compatibility Matrix v1.0 (Ledger 7.0.0):
- Node: `midnightntwrk/midnight-node:0.20.1`
- Indexer: `midnightntwrk/indexer-standalone:3.0.0`
- Proof Server: `midnightnetwork/proof-server:7.0.0-rc.1`

## Container GCloud Credentials

The containers need access to GCloud credentials for Vertex AI. This is handled by mounting the local ADC credentials:

```yaml
# compose.yaml - somnia-agent service
environment:
  - GOOGLE_APPLICATION_CREDENTIALS=/creds/application_default_credentials.json
volumes:
  - ${HOME}/.config/gcloud:/creds:ro
```

**Prerequisites:**
```bash
# Ensure ADC is configured locally
gcloud auth application-default login
```

## Unified LLM Configuration

All LLM configuration is centralized in `config.toml`. Each component can reference its preferred LLM provider, with a default fallback.

### Config Structure

```toml
# config.toml

# Default provider for components not explicitly assigned
[default.llm]
default = "vertex-gemini"

# Available providers
[default.llm.providers.vertex-gemini]
type = "vertex"
project = "kunal-scratch"
location = "us-central1"
model = "gemini-2.0-flash"

[default.llm.providers.vertex-anthropic]
type = "vertex-anthropic"
project = "kunal-scratch"
location = "us-east5"
model = "claude-sonnet-4"

# Component assignments (omitted = uses default)
[default.llm.components]
somnia-agent = "vertex-gemini"
sonic-agent = "vertex-gemini"
midnight-agent = "vertex-gemini"
opencode = "vertex-anthropic"
```

### How It Works

1. **`config.toml`** defines reusable LLM providers and assigns them to components
2. **`scripts/generate-env.ts`** reads config.toml and generates `.env.generated`:
   - Component-specific vars: `SOMNIA_AGENT_LLM_PROJECT`, `SOMNIA_AGENT_LLM_LOCATION`, etc.
   - OpenCode-specific vars: `GOOGLE_CLOUD_PROJECT`, `VERTEX_LOCATION`, `GOOGLE_CLOUD_LOCATION`
   - **Important:** `GOOGLE_CLOUD_LOCATION` is set to OpenCode's provider location (not default)
     because OpenCode reads `GOOGLE_CLOUD_LOCATION` before `VERTEX_LOCATION`
3. **`compose.yaml`** loads `.env.generated` via `env_file`
4. **Components** read from their component-specific env vars, with fallback to generic vars

### Generated Environment Variables

```bash
# .env.generated (auto-generated)

# somnia-agent → vertex-anthropic
SOMNIA_AGENT_LLM_TYPE=vertex-anthropic
SOMNIA_AGENT_LLM_PROJECT=kunal-scratch
SOMNIA_AGENT_LLM_LOCATION=global
SOMNIA_AGENT_LLM_MODEL=claude-opus-4-5

# opencode → vertex-anthropic
OPENCODE_LLM_TYPE=vertex-anthropic
OPENCODE_LLM_PROJECT=kunal-scratch
OPENCODE_LLM_LOCATION=global
OPENCODE_LLM_MODEL=claude-opus-4-5

# OpenCode-specific (reads these env vars)
# GOOGLE_CLOUD_LOCATION is set to OpenCode's provider location
# because OpenCode reads GOOGLE_CLOUD_LOCATION before VERTEX_LOCATION
GOOGLE_CLOUD_PROJECT=kunal-scratch
VERTEX_LOCATION=global
GOOGLE_CLOUD_LOCATION=global
```

### Why Different Regions?

| Provider | Model | Supported Regions |
|----------|-------|-------------------|
| `vertex` | Gemini | `us-central1`, `us-east4`, etc. |
| `vertex-anthropic` | Claude | `us-east5`, `europe-west1`, etc. |

Claude via Vertex AI is only available in specific regions (`us-east5`, `europe-west1`), while Gemini works in most regions.

### Adding a New Component

1. Add to `config.toml`:
   ```toml
   [default.llm.components]
   my-new-agent = "vertex-gemini"  # or create a new provider
   ```

2. Run `bun scripts/generate-env.ts` to regenerate `.env.generated`

3. In your component code, read from env vars:
   ```typescript
   const project = process.env.MY_NEW_AGENT_LLM_PROJECT || process.env.GOOGLE_CLOUD_PROJECT;
   const location = process.env.MY_NEW_AGENT_LLM_LOCATION || process.env.GOOGLE_CLOUD_LOCATION;
   const model = process.env.MY_NEW_AGENT_LLM_MODEL || 'gemini-2.0-flash';
   ```

## Cloud Deployment (Cloud Run)

### Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Cloud Build (Manual)                      │
│  make cloud-deploy                                          │
│  - Builds 3 images → Artifact Registry                      │
│  - Deploys to Cloud Run in dependency order                 │
└─────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   Cloud Run     │  │   Cloud Run     │  │   Cloud Run     │
│ agent-registry  │  │  somnia-agent   │  │  opencode-web   │
│                 │  │                 │  │                 │
│ PUBLIC (no auth)│  │ PUBLIC (no auth)│  │ PUBLIC (no auth)│
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### Configuration

| Item | Value |
|------|-------|
| Project | `kunal-scratch` |
| Region | `us-central1` |
| Artifact Registry | `us-central1-docker.pkg.dev/kunal-scratch/coding-labs` |

### Deployment Workflow

```bash
# First time - one-time setup
make cloud-setup

# Deploy all services
make cloud-deploy

# View service URLs
make cloud-urls

# View logs
make cloud-logs              # opencode-web (default)
make cloud-logs SVC=somnia-agent

# Check status
make cloud-status

# Delete all services
make cloud-delete
```

### Deployment Order

Cloud Build deploys services in dependency order:
1. **agent-registry** (no dependencies) → get URL
2. **somnia-agent** (needs AGENT_REGISTRY_URL) → get URL
3. **opencode-web** (needs both URLs + Vertex AI config)
4. **update-registry** (passes SOMNIA_AGENT_URL back to agent-registry)

### Environment Variables

| Service | Env Vars Set by Cloud Build |
|---------|---------------------------|
| agent-registry | `NODE_ENV`, `SOMNIA_AGENT_URL` (via update step) |
| somnia-agent | `NODE_ENV`, `AGENT_REGISTRY_URL` |
| opencode-web | `NODE_ENV`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_LOCATION`, `AGENT_REGISTRY_URL`, `SOMNIA_AGENT_URL` |

### Files

| File | Purpose |
|------|---------|
| `cloudbuild.yaml` | Cloud Build pipeline definition |
| `scripts/cloud.sh` | Cloud deployment commands |

### IAP Authentication (Google Sign-In)

OpenCode web uses Google's Identity-Aware Proxy (IAP) to restrict access to whitelisted users.

#### How It Works

```
User visits opencode-web URL
    ↓
IAP: Is user authenticated? → NO → Redirect to Google Sign-In
    ↓
IAP: Is user in whitelist? → NO → "Access denied" page
    ↓
YES → OpenCode loads, /global/me returns user email
```

#### User Management

Allowed users are defined in `config.toml`:

```toml
[default.auth]
allowed_users = [
  "user1@gmail.com",
  "user2@example.com",
]
```

**To add/remove users:**
```bash
# 1. Edit config.toml
# 2. Sync to IAP
make cloud-sync-users
```

#### Local Development

When running locally (no IAP), the `/global/me` endpoint uses `gcloud config get account` to get the developer's Google account. This provides a consistent experience between local and production.

#### Files

| File | Purpose |
|------|---------|
| `config.toml` → `[default.auth]` | Allowed users list |
| `scripts/cloud.sh sync-users` | Syncs config.toml to IAP bindings |
| `opencode/.../routes/global.ts` | `/global/me` endpoint |
| `opencode/.../context/auth.tsx` | Frontend auth context |
| `opencode/.../components/auth-button.tsx` | Auth button in titlebar |

### TODO: Security Lockdown

Internal services (agent-registry, somnia-agent) are still public. Future work:
- [ ] Change agent-registry to `--ingress=internal --no-allow-unauthenticated`
- [ ] Change somnia-agent to `--ingress=internal --no-allow-unauthenticated`
- [ ] Add IAM token logic to OpenCode A2A client for service-to-service auth
- [ ] Grant opencode-web SA `roles/run.invoker` on internal services
- [ ] Create dedicated service account (currently using default Compute SA)

### Live Service URLs

| Service | URL |
|---------|-----|
| agent-registry | https://agent-registry-2rejqy6csq-uc.a.run.app |
| somnia-agent | https://somnia-agent-2rejqy6csq-uc.a.run.app |
| opencode-web | https://opencode-web-2rejqy6csq-uc.a.run.app |

## dCoder Login Page

The login page provides a branded entry point for users before they sign in with Google.

### Architecture

```
┌──────────────────────┐         ┌──────────────────────────┐
│   opencode-login     │         │      opencode-web        │
│   (Cloud Run)        │         │   (Cloud Run + IAP)      │
│   Public             │  ──→    │   IAP Protected          │
│                      │  click  │                          │
│   "Sign in with      │         │   Google OAuth handled   │
│    Google" button    │         │   automatically by IAP   │
└──────────────────────┘         └──────────────────────────┘
```

### Local Development

```bash
# Dev mode with Vite hot reload (recommended)
./scripts/dev.sh run-all                    # Frontend: http://localhost:3000

# Production-like (pre-built frontend)
./scripts/dev.sh run-all-built              # Frontend: http://localhost:4097

# With login page
./scripts/dev.sh run-all-with-login         # Login: http://localhost:4098 → App: http://localhost:3000
./scripts/dev.sh run-all-with-login-built   # Login: http://localhost:4098 → App: http://localhost:4097

# With containers (optional login profile)
docker compose up -d                    # Without login page
docker compose --profile login up -d    # With login page
```

### Run Mode Comparison

| Command | Frontend | Access URL | Use Case |
|---------|----------|------------|----------|
| `run-all` | Vite dev server | http://localhost:3000 | Development (hot reload) |
| `run-all-built` | Pre-built static | http://localhost:4097 | Production-like testing |
| `run-all-with-login` | Vite dev server | http://localhost:4098 | Dev with login flow |
| `run-all-with-login-built` | Pre-built static | http://localhost:4098 | Prod-like with login |

### Environment Variables

| Variable | Service | Purpose |
|----------|---------|---------|
| `APP_URL` | opencode-login | URL of IAP-protected app (for "Sign in" button) |
| `LOGIN_PAGE_URL` | opencode-web | URL of login page (for sign-out redirect) |

### Files

| File | Purpose |
|------|---------|
| `packages/opencode-login/` | Login page service |
| `packages/opencode-login/public/index.html` | Landing page with ASCII art |
| `packages/opencode-login/public/styles.css` | Dark theme, orange accents |
| `packages/opencode-login/server.js` | Express static server |
| `packages/opencode-login/Containerfile` | Container build |
| `opencode/.../routes/global.ts` | `/global/app-config` endpoint |
| `opencode/.../context/auth.tsx` | Sign-out redirect logic |

---
*Last updated: 2026-02-18 (session 18)*
