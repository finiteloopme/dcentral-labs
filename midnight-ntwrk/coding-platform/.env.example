# Midnight Development Platform - Environment Configuration
# Copy this file to .env and customize for your environment

# Container Configuration
IMAGE_NAME=midnight-workstation
IMAGE_TAG=latest
CONTAINER_NAME=midnight-local

# Port Configuration
APP_PORT=3000
PROOF_SERVICE_PORT=8080
CODE_PORT=8443
TERMINAL_PORT=7681

# Proof Service Configuration
# Mode: local (default) or external
PROOF_SERVICE_MODE=local

# Local proof server settings (when PROOF_SERVICE_MODE=local)
PROOF_SERVICE_HOST=0.0.0.0
PROOF_SERVICE_LOG_LEVEL=info
PROOF_SERVICE_THREADS=4
PROOF_SERVICE_CACHE_SIZE=1000

# External proof service URL (when PROOF_SERVICE_MODE=external)
# PROOF_SERVICE_URL=https://proof-api.midnight.network
# PROOF_SERVICE_URL=https://your-proof-service.com

# GCP Configuration
# PROJECT_ID=your-gcp-project
# REGION=us-central1
# ZONE=us-central1-a
# GCP_PROJECT_ID=your-gcp-project  # For Vertex AI in OpenCode
# MOUNT_GCLOUD_CREDS=true          # Mount local gcloud credentials in run-local
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json  # Optional: service account key

# Development Environment
MIDNIGHT_ENV=local

# Node.js Configuration
NODE_ENV=development

# OpenCode Configuration
OPENCODE_OPENEDITOR=false
OPENCODE_NOEDITOR=true
EDITOR=nano
BROWSER=none