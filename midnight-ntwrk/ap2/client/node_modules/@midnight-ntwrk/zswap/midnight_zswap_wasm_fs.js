  export * from "./midnight_zswap_wasm_bg.js";
  import * as exports from "./midnight_zswap_wasm_bg.js";
  import { __wbg_set_wasm } from "./midnight_zswap_wasm_bg.js";
  import { readFileSync } from 'fs';
  import { join, dirname } from 'path';
  import { fileURLToPath } from 'url';
  
  let imports = {};
  imports['./midnight_zswap_wasm_bg.js'] = exports;
import * as snippets_midnight_zswap_wasm_41bcd0561f7a9007_inline0_js from './snippets/midnight-zswap-wasm-41bcd0561f7a9007/inline0.js';
imports['./snippets/midnight-zswap-wasm-41bcd0561f7a9007/inline0.js'] = snippets_midnight_zswap_wasm_41bcd0561f7a9007_inline0_js;
  
  const __filename = fileURLToPath(import.meta.url);
  const __dirname = dirname(__filename);
  const wasmPath = join(__dirname, 'midnight_zswap_wasm_bg.wasm');
  const bytes = readFileSync(wasmPath);
  
  const wasmModule = new WebAssembly.Module(bytes);
  const wasmInstance = new WebAssembly.Instance(wasmModule, imports);
  const wasm = wasmInstance.exports;
  
  __wbg_set_wasm(wasm);
  wasm.__wbindgen_start();
