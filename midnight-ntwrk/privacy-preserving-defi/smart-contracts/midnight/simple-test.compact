pragma language_version >= 0.14.0;

export ledger total_value_locked: U64;

constructor() {
    total_value_locked = 0;
}

export circuit deposit(amount: U64): [] {
    total_value_locked = total_value_locked + amount;
}