apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midnight-node.fullname" . }}-test-connection"
  labels:
    {{- include "midnight-node.selectorLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midnight-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
