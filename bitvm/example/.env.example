# BitVM3 Configuration

# Verification Engine API Configuration
PORT=8080                          # API server port (default: 8080)
RUST_LOG=info                      # Logging level (debug, info, warn, error)

# Client Configuration (for vault-protocol)
RUST_API_URL=http://localhost:8080 # Verification Engine API URL

# Bitcoin Network Configuration
BITCOIN_NETWORK=regtest            # Network: mainnet, testnet, or regtest
BITCOIN_RPC_URL=http://localhost:18443
BITCOIN_RPC_USER=bitvm3
BITCOIN_RPC_PASSWORD=password

# Optional: Database Configuration
DATABASE_URL=postgresql://localhost/bitvm3
REDIS_URL=redis://localhost:6379